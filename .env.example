# このファイルを .env としてコピーし、あなたの環境に合わせて値を設定してください。
# Copy this file to .env and set the values for your environment.

# --- シスオペアカウント設定 / SysOp Account Settings ---
# 初回起動時に作成されるシステム管理者アカウントの情報です。
# Information for the system administrator account created on first launch.
# 本番環境で運用する場合は、必ず安全なIDとパスワードに変更してください。GUESTやADMINなどの推測されやすいIDは避けてください。
# For production environments, be sure to change these to a secure ID and password. Avoid easily guessable IDs like GUEST or ADMIN.
GRASSROOTSBBS_SYSOP_ID=your_secure_sysop_id
GRASSROOTSBBS_SYSOP_PASSWORD=your_very_strong_password
GRASSROOTSBBS_SYSOP_EMAIL=sysop@example.com

# --- データベース接続情報 / Database Connection ---
# Docker Composeを使用する場合、通常は変更不要です。
# If using Docker Compose, these usually do not need to be changed.
# 本番環境で運用する場合は、DB_PASSWORDとMYSQL_ROOT_PASSWORDをより安全なものに変更することを強く推奨します。
# For production environments, it is strongly recommended to change DB_PASSWORD and MYSQL_ROOT_PASSWORD to something more secure.
DB_HOST=db
DB_USER=grbbs_user
DB_PASSWORD=your_secure_database_password
DB_NAME=grbbs

# --- データベースのrootパスワード / Database Root Password ---
# MariaDBコンテナのrootユーザーのパスワードです。
# For production environments, it is strongly recommended to change this to something secure.
MYSQL_ROOT_PASSWORD=your_secret_root_password

# --- ファイルパーミッション用のユーザー/グループID / User/Group ID for file permissions ---
# コンテナ内で作成されるファイルの所有者をホストのユーザーと一致させるために使用します。
# Linux/macOSでは `id -u` と `id -g` コマンドで確認できます。
# Used to match file ownership between the container and the host.
# On Linux/macOS, you can find these with `id -u` and `id -g`.
UID=1000
GID=1000